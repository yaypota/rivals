local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/Kavo%20UI.lua"))()
local Window = Library.CreateLib("Canvas - Left", "DarkTheme")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local Camera = workspace.CurrentCamera

local ragebotEnabled = false
local ragebotFOV = 180
local ragebotHitChance = 90
local ragebotPrediction = true
local ragebotPredictionMultiplier = 1.2
local ragebotWallbang = false
local ragebotAutoShoot = false
local ragebotTriggerbot = false
local ragebotTriggerDelay = 0.1
local ragebotMultiTarget = false
local ragebotHumanization = false
local ragebotFOVCircle = false
local ragebotFOVColor = Color3.fromRGB(255, 0, 0)
local ragebotFOVSize = 180

local voidSpamEnabled = false
local voidSpamDelay = 0.5
local voidSpamRange = 50
local voidSpamAutoTarget = false
local voidSpamMultiCast = false
local voidSpamCastCount = 1
local voidSpamTargetAll = false
local voidSpamSilentCast = false
local voidSpamAOE = false
local voidSpamAOERadius = 15
local voidSpamRandomize = false
local voidSpamCooldownReduction = false
local voidSpamEnergyBoost = false

local dodgeEnabled = false
local dodgeMode = "Auto"
local dodgeRadius = 15
local dodgePredictionTime = 0.5
local dodgeSmoothness = 0.3
local dodgeOnlyCritical = false
local dodgeDistance = 10
local dodgeDirection = "Random"
local dodgeCooldown = 0.5
local dodgeLastDodge = 0

local flyEnabled = false
local flySpeed = 50
local noclipEnabled = false
local speedEnabled = false
local walkSpeed = 16
local infiniteJumpEnabled = false
local jumpPower = 50

local voidSpamConnection = nil
local fovCircle = nil
local projectileCache = {}
local targetTable = {}
local voidSpamTimer = 0
local lastUpdate = 0

local function GetCharacter()
    local char = LocalPlayer.Character
    if not char then return nil, nil, nil end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil, nil, nil end
    local humanoid = char:FindFirstChild("Humanoid")
    if not humanoid then return nil, nil, nil end
    return char, hrp, humanoid
end

local function UpdateProjectileCache()
    for i = #projectileCache, 1, -1 do table.remove(projectileCache, i) end
    local char, hrp = GetCharacter()
    if not char or not hrp then return end
    local playerPos = hrp.Position
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("BasePart") and obj.Parent and not obj.Parent:IsA("Player") then
            local isProjectile = false
            local name = obj.Name:lower()
            if name:find("projectile") or name:find("bullet") or name:find("missile") or 
               name:find("rocket") or name:find("ball") or name:find("orb") or
               name:find("shot") or name:find("beam") or name:find("laser") or name:find("void") then
                isProjectile = true
            end
            if not isProjectile and obj.Velocity and obj.Velocity.Magnitude > 10 then
                isProjectile = true
            end
            if isProjectile then
                local distance = (playerPos - obj.Position).Magnitude
                if distance < dodgeRadius then
                    table.insert(projectileCache, {
                        object = obj,
                        position = obj.Position,
                        velocity = obj.Velocity,
                        distance = distance
                    })
                end
            end
        end
    end
end

local function GetRageTargets()
    for i = #targetTable, 1, -1 do table.remove(targetTable, i) end
    local char, hrp = GetCharacter()
    if not char or not hrp then return targetTable end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            local targetHrp = player.Character.HumanoidRootPart
            local distance = (hrp.Position - targetHrp.Position).Magnitude
            table.insert(targetTable, {
                player = player,
                hrp = targetHrp,
                distance = distance
            })
        end
    end
    table.sort(targetTable, function(a, b) return a.distance < b.distance end)
    return targetTable
end

local function FireRageShot(targetPos)
    local char, hrp = GetCharacter()
    if not char or not hrp then return end
    local remote = ReplicatedStorage:FindFirstChild("ShootEvent") or ReplicatedStorage:FindFirstChild("Attack")
    if not remote then return end
    pcall(function() remote:FireServer(targetPos) end)
    if ragebotHumanization then
        local randomOffset = Vector3.new(
            math.random(-5, 5) / 10,
            math.random(-5, 5) / 10,
            math.random(-5, 5) / 10
        )
        pcall(function() remote:FireServer(targetPos + randomOffset) end)
    end
end

local function Ragebot()
    if not ragebotEnabled then return end
    local char, hrp = GetCharacter()
    if not char or not hrp then return end
    local targets = GetRageTargets()
    if #targets == 0 then return end
    local bestTarget = nil
    local bestScore = -math.huge
    for _, target in ipairs(targets) do
        if target.distance <= ragebotFOV then
            local score = 1000 - target.distance
            if math.random(1, 100) <= ragebotHitChance then score = score * 1.5 end
            if score > bestScore then
                bestScore = score
                bestTarget = target
            end
        end
    end
    if bestTarget then
        local targetPos
        if ragebotPrediction then
            local velocity = bestTarget.hrp.Velocity
            targetPos = bestTarget.hrp.Position + (velocity * ragebotPredictionMultiplier)
        else
            targetPos = bestTarget.hrp.Position
        end
        FireRageShot(targetPos)
        if ragebotWallbang then
            local remote = ReplicatedStorage:FindFirstChild("WallbangEvent")
            if remote then pcall(function() remote:FireServer(bestTarget.player) end) end
        end
        if ragebotAutoShoot then
            local tool = char:FindFirstChildOfClass("Tool")
            if tool and bestTarget.distance < 100 then pcall(function() tool:Activate() end) end
        end
        if ragebotTriggerbot then
            local screenPos, onScreen = Camera:WorldToScreenPoint(bestTarget.hrp.Position)
            if onScreen then
                local dist = (Vector2.new(Mouse.X, Mouse.Y) - Vector2.new(screenPos.X, screenPos.Y)).Magnitude
                if dist < 100 then
                    task.wait(ragebotTriggerDelay)
                    FireRageShot(bestTarget.hrp.Position)
                end
            end
        end
        if ragebotMultiTarget and #targets > 1 then
            for i = 2, math.min(3, #targets) do
                local target = targets[i]
                if target and target.hrp then
                    FireRageShot(target.hrp.Position + Vector3.new(math.random(-5, 5), math.random(-5, 5), math.random(-5, 5)))
                end
            end
        end
    end
end

local function DrawFOVCircle()
    if not ragebotFOVCircle then
        if fovCircle then fovCircle.Visible = false end
        return
    end
    if not fovCircle then
        fovCircle = Drawing.new("Circle")
        fovCircle.Thickness = 2
        fovCircle.Filled = false
        fovCircle.Transparency = 0.5
    end
    fovCircle.Radius = ragebotFOVSize / 2
    fovCircle.Color = ragebotFOVColor
    fovCircle.Visible = true
    fovCircle.Position = Vector2.new(Mouse.X, Mouse.Y)
end

local function PredictProjectile(projectile)
    if not projectile then return nil end
    local char, hrp = GetCharacter()
    if not char or not hrp then return nil end
    local playerPos = hrp.Position
    local projPos = projectile.position
    local projVel = projectile.velocity
    if projVel.Magnitude < 1 then return nil end
    local dirToPlayer = (playerPos - projPos).Unit
    local projDir = projVel.Unit
    local angle = math.acos(math.clamp(dirToPlayer:Dot(projDir), -1, 1))
    local relativeVelocity = projVel - Vector3.new(0, 0, 0)
    local timeToImpact = (playerPos - projPos).Magnitude / (relativeVelocity.Magnitude + 0.001)
    local isCritical = false
    if timeToImpact < 1 and angle < math.rad(45) then isCritical = true end
    return { timeToImpact = timeToImpact, isCritical = isCritical }
end

local function GetDodgeDirection(projectile)
    if dodgeDirection == "Random" then
        local angle = math.random(0, 360)
        local rad = math.rad(angle)
        return Vector3.new(math.cos(rad), 0, math.sin(rad)) * dodgeDistance
    elseif dodgeDirection == "Left" then
        return -Camera.CFrame.RightVector * dodgeDistance
    elseif dodgeDirection == "Right" then
        return Camera.CFrame.RightVector * dodgeDistance
    elseif dodgeDirection == "Back" then
        return -Camera.CFrame.LookVector * dodgeDistance
    elseif dodgeDirection == "Forward" then
        return Camera.CFrame.LookVector * dodgeDistance
    else
        local projDir = projectile.velocity.Unit
        local perpDir = Vector3.new(-projDir.Z, 0, projDir.X)
        if math.random(1, 2) == 1 then perpDir = -perpDir end
        return perpDir * dodgeDistance
    end
end

local function PerformDodge(direction)
    local char, hrp = GetCharacter()
    if not char or not hrp then return end
    local targetPos = hrp.Position + direction
    local tweenInfo = TweenInfo.new(dodgeSmoothness, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
    pcall(function()
        local tween = TweenService:Create(hrp, tweenInfo, {Position = targetPos})
        tween:Play()
    end)
    dodgeLastDodge = tick()
end

local function Dodger()
    if not dodgeEnabled then return end
    local char, hrp = GetCharacter()
    if not char or not hrp then return end
    if tick() - dodgeLastDodge < dodgeCooldown then return end
    if tick() - lastUpdate > 1 then
        UpdateProjectileCache()
        lastUpdate = tick()
    end
    if #projectileCache == 0 then return end
    local shouldDodge = false
    local dangerLevel = 0
    for _, proj in ipairs(projectileCache) do
        local prediction = PredictProjectile(proj)
        if prediction then
            if prediction.isCritical then dangerLevel = dangerLevel + 2 else dangerLevel = dangerLevel + 1 end
            if dodgeMode == "Auto" then
                shouldDodge = true
            elseif dodgeMode == "Smart" then
                if prediction.timeToImpact < 0.5 or prediction.isCritical then shouldDodge = true end
            elseif dodgeMode == "Manual" then
                if UserInputService:IsKeyDown(Enum.KeyCode.X) then shouldDodge = true end
            end
            if shouldDodge then
                local dir = GetDodgeDirection(proj)
                if dir then PerformDodge(dir) return end
            end
        end
    end
    if dodgeOnlyCritical and dangerLevel < 2 then return end
end

local function GetRandomTarget(targets)
    if #targets == 0 then return nil end
    return targets[math.random(1, #targets)]
end

local function CastVoidSpell(target)
    if not target or not target.Character then return end
    local char, hrp = GetCharacter()
    if not char or not hrp then return end
    local remote = ReplicatedStorage:FindFirstChild("VoidSpellEvent") or ReplicatedStorage:FindFirstChild("CastSpell")
    if not remote then return end
    local targetPos = target.Character.HumanoidRootPart.Position
    pcall(function() remote:FireServer(targetPos, target) end)
    if voidSpamCooldownReduction then
        local cdRemote = ReplicatedStorage:FindFirstChild("CooldownReduction")
        if cdRemote then pcall(function() cdRemote:FireServer() end) end
    end
    if voidSpamEnergyBoost then
        local enRemote = ReplicatedStorage:FindFirstChild("EnergyBoost")
        if enRemote then pcall(function() enRemote:FireServer() end) end
    end
end

local function VoidSpam()
    if not voidSpamEnabled then return end
    local char, hrp = GetCharacter()
    if not char or not hrp then return end
    local targets = {}
    if voidSpamAutoTarget or voidSpamTargetAll then
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local distance = (hrp.Position - player.Character.HumanoidRootPart.Position).Magnitude
                if distance <= voidSpamRange then table.insert(targets, player) end
            end
        end
    end
    if voidSpamAOE then
        local remote = ReplicatedStorage:FindFirstChild("VoidAOEEvent")
        if remote then
            local centerPos = hrp.Position
            for _, target in ipairs(targets) do
                if target.Character then
                    local targetPos = target.Character.HumanoidRootPart.Position
                    if (centerPos - targetPos).Magnitude <= voidSpamAOERadius then
                        pcall(function() remote:FireServer(targetPos, target, voidSpamAOERadius) end)
                    end
                end
            end
        end
    end
    if voidSpamMultiCast then
        for i = 1, voidSpamCastCount do
            if voidSpamTargetAll then
                for _, target in ipairs(targets) do
                    CastVoidSpell(target)
                    if voidSpamRandomize then task.wait(math.random(1, 10) / 100) end
                end
            else
                local target = GetRandomTarget(targets)
                if target then CastVoidSpell(target) end
            end
        end
    else
        if voidSpamTargetAll then
            for _, target in ipairs(targets) do CastVoidSpell(target) end
        else
            local target = GetRandomTarget(targets)
            if target then CastVoidSpell(target) end
        end
    end
    if voidSpamSilentCast then
        local remote = ReplicatedStorage:FindFirstChild("SilentCastEvent")
        if remote then
            for _, target in ipairs(targets) do
                if target.Character then pcall(function() remote:FireServer(target) end) end
            end
        end
    end
end

local MainTab = Window:NewTab("Main")
local VisualTab = Window:NewTab("Visual")
local PlayerTab = Window:NewTab("Player")
local SettingsTab = Window:NewTab("Settings")

local MainLeftGroup = MainTab:NewSection("Ragebot")
local MainCenterGroup = MainTab:NewSection("Dodge System")
local MainRightGroup = MainTab:NewSection("Void Spam")

MainLeftGroup:NewToggle("Enable Ragebot", "Toggle Ragebot", function(v) ragebotEnabled = v end)
MainLeftGroup:NewSlider("Rage FOV", "FOV for Ragebot", 360, 30, 180, function(v) ragebotFOV = v ragebotFOVSize = v end)
MainLeftGroup:NewSlider("Hit Chance", "Hit chance percentage", 100, 10, 90, function(v) ragebotHitChance = v end)
MainLeftGroup:NewToggle("Prediction", "Enable prediction", function(v) ragebotPrediction = v end)
MainLeftGroup:NewToggle("Wallbang", "Enable wallbang", function(v) ragebotWallbang = v end)
MainLeftGroup:NewToggle("Auto Shoot", "Auto shoot", function(v) ragebotAutoShoot = v end)
MainLeftGroup:NewToggle("Triggerbot", "Enable triggerbot", function(v) ragebotTriggerbot = v end)
MainLeftGroup:NewToggle("Multi Target", "Multi target mode", function(v) ragebotMultiTarget = v end)
MainLeftGroup:NewToggle("Humanization", "Human-like aim", function(v) ragebotHumanization = v end)

MainCenterGroup:NewToggle("Enable Dodge", "Toggle dodge system", function(v) 
    dodgeEnabled = v
    if v then UpdateProjectileCache() end
end)
MainCenterGroup:NewDropdown("Dodge Mode", "Select dodge mode", {"Auto", "Smart", "Manual"}, function(v) dodgeMode = v end)
MainCenterGroup:NewSlider("Detection Radius", "Detection radius for projectiles", 50, 5, 15, function(v) 
    dodgeRadius = v
    UpdateProjectileCache()
end)
MainCenterGroup:NewSlider("Dodge Distance", "Distance to dodge", 30, 3, 10, function(v) dodgeDistance = v end)
MainCenterGroup:NewSlider("Prediction Time", "Prediction time for projectiles", 2, 0.1, 0.5, function(v) dodgePredictionTime = math.floor(v * 10) / 10 end)
MainCenterGroup:NewSlider("Dodge Smoothness", "Smoothness of dodge", 1, 0.05, 0.3, function(v) dodgeSmoothness = math.floor(v * 100) / 100 end)
MainCenterGroup:NewSlider("Dodge Cooldown", "Cooldown between dodges", 2, 0.1, 0.5, function(v) dodgeCooldown = math.floor(v * 10) / 10 end)
MainCenterGroup:NewDropdown("Dodge Direction", "Direction to dodge", {"Random", "Left", "Right", "Back", "Forward", "Smart"}, function(v) dodgeDirection = v end)
MainCenterGroup:NewToggle("Critical Only", "Only dodge critical hits", function(v) dodgeOnlyCritical = v end)

MainRightGroup:NewToggle("Enable Void Spam", "Toggle Void Spam", function(v)
    voidSpamEnabled = v
    if v then
        if not voidSpamConnection then
            voidSpamTimer = 0
            voidSpamConnection = RunService.Heartbeat:Connect(function(deltaTime)
                if voidSpamEnabled then
                    voidSpamTimer = voidSpamTimer + deltaTime
                    if voidSpamTimer >= voidSpamDelay then
                        voidSpamTimer = 0
                        VoidSpam()
                    end
                end
            end)
        end
    else
        if voidSpamConnection then
            voidSpamConnection:Disconnect()
            voidSpamConnection = nil
            voidSpamTimer = 0
        end
    end
end)
MainRightGroup:NewDropdown("Void Mode", "Select void mode", {"Normal", "Rapid", "Hyper", "Ultimate"}, function(v)
    if v == "Normal" then
        voidSpamDelay = 0.5
        voidSpamMultiCast = false
        voidSpamCastCount = 1
    elseif v == "Rapid" then
        voidSpamDelay = 0.2
        voidSpamMultiCast = true
        voidSpamCastCount = 3
    elseif v == "Hyper" then
        voidSpamDelay = 0.05
        voidSpamMultiCast = true
        voidSpamCastCount = 5
    elseif v == "Ultimate" then
        voidSpamDelay = 0.01
        voidSpamMultiCast = true
        voidSpamCastCount = 10
        voidSpamAOE = true
        voidSpamAOERadius = 30
    end
end)
MainRightGroup:NewSlider("Spam Range", "Range for void spam", 200, 10, 50, function(v) voidSpamRange = v end)
MainRightGroup:NewSlider("Spam Delay", "Delay between casts", 2, 0.01, 0.5, function(v) voidSpamDelay = math.floor(v * 100) / 100 end)
MainRightGroup:NewToggle("Auto Target", "Auto target enemies", function(v) voidSpamAutoTarget = v end)
MainRightGroup:NewToggle("Target All", "Target all enemies", function(v) voidSpamTargetAll = v end)
MainRightGroup:NewToggle("Multi Cast", "Multi cast spells", function(v) voidSpamMultiCast = v end)
MainRightGroup:NewToggle("Silent Cast", "Silent casting", function(v) voidSpamSilentCast = v end)
MainRightGroup:NewToggle("AOE", "Area of effect", function(v) voidSpamAOE = v end)
MainRightGroup:NewToggle("Randomize", "Randomize casts", function(v) voidSpamRandomize = v end)
MainRightGroup:NewToggle("Cooldown Reduction", "Reduce cooldowns", function(v) voidSpamCooldownReduction = v end)
MainRightGroup:NewToggle("Energy Boost", "Boost energy", function(v) voidSpamEnergyBoost = v end)

local VisualLeftGroup = VisualTab:NewSection("FOV Circle")
local VisualRightGroup = VisualTab:NewSection("Visual Options")

VisualLeftGroup:NewToggle("FOV Circle", "Show FOV circle", function(v)
    ragebotFOVCircle = v
    if not v and fovCircle then fovCircle.Visible = false end
end)
VisualLeftGroup:NewColorPicker("FOV Color", "FOV circle color", Color3.fromRGB(255, 0, 0), function(v) ragebotFOVColor = v end)
VisualLeftGroup:NewSlider("FOV Size", "Size of FOV circle", 360, 50, 180, function(v)
    ragebotFOVSize = v
    ragebotFOV = v
end)

VisualRightGroup:NewToggle("Enable ESP", "Enable ESP", function(v) end)
VisualRightGroup:NewColorPicker("ESP Color", "ESP color", Color3.fromRGB(255, 255, 255), function(v) end)
VisualRightGroup:NewToggle("Chams", "Enable chams", function(v) end)
VisualRightGroup:NewToggle("Name Tags", "Show name tags", function(v) end)

local PlayerLeftGroup = PlayerTab:NewSection("Player Stats")
local PlayerRightGroup = PlayerTab:NewSection("Player Options")

PlayerLeftGroup:NewLabel("Username: " .. LocalPlayer.Name)
local healthLabel = PlayerLeftGroup:NewLabel("Health: N/A")
local maxHealthLabel = PlayerLeftGroup:NewLabel("Max Health: N/A")
local positionLabel = PlayerLeftGroup:NewLabel("Position: N/A")

PlayerRightGroup:NewToggle("Fly Mode", "Toggle fly mode", function(v)
    flyEnabled = v
    local char, hrp, humanoid = GetCharacter()
    if char and humanoid then humanoid.PlatformStand = v end
end)
PlayerRightGroup:NewSlider("Fly Speed", "Fly speed", 200, 10, 50, function(v) flySpeed = v end)
PlayerRightGroup:NewToggle("Noclip", "Toggle noclip", function(v)
    noclipEnabled = v
    local char, hrp = GetCharacter()
    if char then
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = not v end
        end
    end
end)
PlayerRightGroup:NewToggle("Speed Boost", "Toggle speed boost", function(v)
    speedEnabled = v
    local char, hrp, humanoid = GetCharacter()
    if char and humanoid then humanoid.WalkSpeed = v and walkSpeed or 16 end
end)
PlayerRightGroup:NewSlider("Walk Speed", "Walk speed", 100, 16, 16, function(v)
    walkSpeed = v
    if speedEnabled then
        local char, hrp, humanoid = GetCharacter()
        if char and humanoid then humanoid.WalkSpeed = v end
    end
end)
PlayerRightGroup:NewToggle("Infinite Jump", "Toggle infinite jump", function(v)
    infiniteJumpEnabled = v
    local char, hrp, humanoid = GetCharacter()
    if char and humanoid then humanoid.JumpPower = v and jumpPower or 50 end
end)
PlayerRightGroup:NewSlider("Jump Power", "Jump power", 200, 50, 50, function(v)
    jumpPower = v
    if infiniteJumpEnabled then
        local char, hrp, humanoid = GetCharacter()
        if char and humanoid then humanoid.JumpPower = v end
    end
end)

local SettingsGroup = SettingsTab:NewSection("Configuration")
local SettingsRightGroup = SettingsTab:NewSection("Theme")

SettingsGroup:NewToggle("Save on Exit", "Save settings on exit", function(v) end)
SettingsGroup:NewButton("Save Config", "Save current config", function() end)
SettingsGroup:NewButton("Load Config", "Load saved config", function() end)
SettingsGroup:NewButton("Reset Config", "Reset config", function() end)

SettingsRightGroup:NewDropdown("UI Theme", "Select UI theme", {"Default", "Dark", "Light", "Neon", "Pastel", "Cyberpunk"}, function(v) end)
SettingsRightGroup:NewButton("Reset Theme", "Reset to default theme", function() end)

RunService.RenderStepped:Connect(function()
    pcall(function()
        if ragebotEnabled then Ragebot() end
        if dodgeEnabled then Dodger() end
        if ragebotFOVCircle then DrawFOVCircle() elseif fovCircle then fovCircle.Visible = false end
        
        local char, hrp, humanoid = GetCharacter()
        if char and humanoid then
            healthLabel:Set("Health: " .. math.floor(humanoid.Health))
            maxHealthLabel:Set("Max Health: " .. math.floor(humanoid.MaxHealth))
        end
        if hrp then
            local pos = hrp.Position
            positionLabel:Set("Position: " .. string.format("%.1f, %.1f, %.1f", pos.X, pos.Y, pos.Z))
        end
        
        if flyEnabled and hrp then
            local moveDirection = Vector3.new()
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDirection = moveDirection + Camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDirection = moveDirection - Camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDirection = moveDirection - Camera.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDirection = moveDirection + Camera.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDirection = moveDirection + Vector3.new(0, 1, 0) end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveDirection = moveDirection - Vector3.new(0, 1, 0) end
            if moveDirection.Magnitude > 0 then
                moveDirection = moveDirection.Unit * flySpeed
                hrp.Velocity = moveDirection
            else
                hrp.Velocity = Vector3.new(0, 0, 0)
            end
        end
    end)
end)
